import { MinimizeDescPipe } from './minimize-desc.pipe';

describe('MinimizeDescPipe', () => {
  it('create an instance', () => {
    const pipe = new MinimizeDescPipe();
    expect(pipe).toBeTruthy();
  });
});
